query Tracks {
    tracks(orderBy: artists_DESC, last: 100) {
      id
      audioFile
      bpm
      coverArt
      description
      downloadLink
      isAvailable
      label
      name
      spotify
      soundcloud
      title
      tone
      youtube
    }
  }
